package threadmanager;

import java.io.File;
import java.io.FileOutputStream;
import java.io.IOException;
import java.io.OutputStream;
import java.util.Properties;

import dao.SQLStrings;

@SuppressWarnings("unused")
public class PropertiesFileConfig {/*
	public static void main(String[] args){
		Properties properties = new Properties();
		
		properties.setProperty("DRIVER", SQLStrings.DRIVER);
		properties.setProperty("DATABASE", SQLStrings.DB_NAME);
		properties.setProperty("FRAMEWORK", SQLStrings.FRAMEWORK);
		properties.setProperty("PROTOCOL", SQLStrings.PROTOCOL);
		
		try(OutputStream os = new FileOutputStream(new File("properties.prop"))){
			properties.storeToXML(os, null);
		} catch (IOException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
	}
	*/
}
